import Automata from '../Automata/Automata';
import DFA from '../Automata/DFA';

/**
 *
 * @param  Left
 * @param  Right
 */
export default function union(Left, Right) {
    return unionDFA(Left, Right)
}

function unionDFA(Left, Right) {

}